# Manuscript / repository synchronization notes

## Required pre-submission synchronization

The v0.17 manuscript correctly states that the primary axolotl score uses **training-frozen** means and SDs. Its Figure 4, however, was inherited from the earlier pooled-validation leave-one-out visualization. The qualitative conclusion is unchanged (HI AUC 1.0 in 5/5; LOW AUC 0.75–1.0), but a public reproducibility release should replace that panel with the training-frozen LOO values generated by `analysis/axolotl/frozen_transfer.py`.

Training-frozen LOW LOO AUCs are: 0.75, 1.0, 0.875, 1.0, 0.875 for omitted samples 82226, 82228, 82221, 82227 and 82229 respectively.

The repository therefore treats the existing frozen Figure 4 as manuscript provenance and the recomputed training-frozen table as the canonical analysis output for the next submission-operational manuscript version.

The killifish remote-dorsal result is now reproduced end-to-end by `analysis/killifish/remote_dorsal_control.py`; older cell-level scripts are clearly labelled legacy/descriptive.

The repository-side `figures/main/Figure_4.png` has therefore been regenerated from the canonical training-frozen LOO table. The next manuscript operational version should use this panel.
